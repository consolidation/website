import{_ as e,o,c as n,d as s}from"./app.53a4418c.js";const a={},i=s(`<h1 id="composer-tasks" tabindex="-1"><a class="header-anchor" href="#composer-tasks" aria-hidden="true">#</a> Composer Tasks</h1><h2 id="checkplatformreqs" tabindex="-1"><a class="header-anchor" href="#checkplatformreqs" aria-hidden="true">#</a> CheckPlatformReqs</h2><p>Composer Check Platform Requirements</p><div class="language-php ext-php line-numbers-mode"><pre class="language-php"><code><span class="token php language-php"><span class="token delimiter important">&lt;?php</span>
<span class="token comment">// simple execution</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerValidate</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token delimiter important">?&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>preferDist($preferDist = null)</code> adds <code>prefer-dist</code> option to composer</li><li><code>preferSource()</code> adds <code>prefer-source</code> option to composer</li><li><code>dev($dev = null)</code> adds <code>dev</code> option to composer</li><li><code>noDev()</code> adds <code>no-dev</code> option to composer</li><li><code>ansi($ansi = null)</code> adds <code>ansi</code> option to composer</li><li><code>noAnsi()</code> adds <code>no-ansi</code> option to composer</li><li><code>interaction($interaction = null)</code> * <code>param bool</code> $interaction</li><li><code>noInteraction()</code> adds <code>no-interaction</code> option to composer</li><li><code>optimizeAutoloader($optimize = null)</code> adds <code>optimize-autoloader</code> option to composer</li><li><code>ignorePlatformRequirements($ignore = null)</code> adds <code>ignore-platform-reqs</code> option to composer</li><li><code>disablePlugins($disable = null)</code> disable plugins</li><li><code>noScripts($disable = null)</code> skip scripts</li><li><code>workingDir($dir)</code> adds <code>--working-dir $dir</code> option to composer</li><li><code>buildCommand()</code> Copy class fields into command options as directed.</li><li><code>setOutput($output)</code> Sets the Console Output.</li><li><code>setProcessInput($input)</code> Pass an input to the process. Can be resource created with fopen() or string</li><li><code>dir($dir)</code> Changes working directory of command</li><li><code>arg($arg)</code> Pass argument to executable. Its value will be automatically escaped.</li><li><code>args($args)</code> Pass methods parameters as arguments to executable. Argument values</li><li><code>rawArg($arg)</code> Pass the provided string in its raw (as provided) form as an argument to executable.</li><li><code>option($option, $value = null, $separator = null)</code> Pass option to executable. Options are prefixed with <code>--</code> , value can be provided in second parameter.</li><li><code>options(array $options, $separator = null)</code> Pass multiple options to executable. The associative array contains</li><li><code>optionList($option, $value = null, $separator = null)</code> Pass an option with multiple values to executable. Value can be a string or array.</li></ul><h2 id="config" tabindex="-1"><a class="header-anchor" href="#config" aria-hidden="true">#</a> Config</h2><p>Composer Config</p><div class="language-php ext-php line-numbers-mode"><pre class="language-php"><code><span class="token php language-php"><span class="token delimiter important">&lt;?php</span>
<span class="token comment">// simple execution</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerConfig</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">set</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;bin-dir&#39;</span><span class="token punctuation">,</span> <span class="token string single-quoted-string">&#39;bin/&#39;</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token delimiter important">?&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>set($key, $value)</code> Set a configuration value.</li><li><code>useGlobal($useGlobal = null)</code> Operate on the global repository</li><li><code>repository($id, $uri, $repoType = null)</code> * <code>param string</code> $id</li><li><code>removeRepository($id)</code> * <code>param string</code> $id</li><li><code>disableRepository($id)</code> * <code>param string</code> $id</li><li><code>enableRepository($id)</code> * <code>param string</code> $id</li><li><code>preferDist($preferDist = null)</code> adds <code>prefer-dist</code> option to composer</li><li><code>preferSource()</code> adds <code>prefer-source</code> option to composer</li><li><code>dev($dev = null)</code> adds <code>dev</code> option to composer</li><li><code>noDev()</code> adds <code>no-dev</code> option to composer</li><li><code>ansi($ansi = null)</code> adds <code>ansi</code> option to composer</li><li><code>noAnsi()</code> adds <code>no-ansi</code> option to composer</li><li><code>interaction($interaction = null)</code> * <code>param bool</code> $interaction</li><li><code>noInteraction()</code> adds <code>no-interaction</code> option to composer</li><li><code>optimizeAutoloader($optimize = null)</code> adds <code>optimize-autoloader</code> option to composer</li><li><code>ignorePlatformRequirements($ignore = null)</code> adds <code>ignore-platform-reqs</code> option to composer</li><li><code>disablePlugins($disable = null)</code> disable plugins</li><li><code>noScripts($disable = null)</code> skip scripts</li><li><code>workingDir($dir)</code> adds <code>--working-dir $dir</code> option to composer</li><li><code>buildCommand()</code> Copy class fields into command options as directed.</li><li><code>setOutput($output)</code> Sets the Console Output.</li><li><code>setProcessInput($input)</code> Pass an input to the process. Can be resource created with fopen() or string</li><li><code>dir($dir)</code> Changes working directory of command</li><li><code>arg($arg)</code> Pass argument to executable. Its value will be automatically escaped.</li><li><code>args($args)</code> Pass methods parameters as arguments to executable. Argument values</li><li><code>rawArg($arg)</code> Pass the provided string in its raw (as provided) form as an argument to executable.</li><li><code>option($option, $value = null, $separator = null)</code> Pass option to executable. Options are prefixed with <code>--</code> , value can be provided in second parameter.</li><li><code>options(array $options, $separator = null)</code> Pass multiple options to executable. The associative array contains</li><li><code>optionList($option, $value = null, $separator = null)</code> Pass an option with multiple values to executable. Value can be a string or array.</li></ul><h2 id="createproject" tabindex="-1"><a class="header-anchor" href="#createproject" aria-hidden="true">#</a> CreateProject</h2><p>Composer CreateProject</p><div class="language-php ext-php line-numbers-mode"><pre class="language-php"><code><span class="token php language-php"><span class="token delimiter important">&lt;?php</span>
<span class="token comment">// simple execution</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerCreateProject</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">source</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;foo/bar&#39;</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">target</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;myBar&#39;</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token delimiter important">?&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>source($source)</code> * <code>param string</code> $source</li><li><code>target($target)</code> * <code>param string</code> $target</li><li><code>version($version)</code> * <code>param string</code> $version</li><li><code>keepVcs($keep = null)</code> * <code>param bool</code> $keep</li><li><code>noInstall($noInstall = null)</code> * <code>param bool</code> $noInstall</li><li><code>repository($repository)</code> * <code>param string</code> $repository</li><li><code>stability($stability)</code> * <code>param string</code> $stability</li><li><code>buildCommand()</code> {@inheritdoc}</li><li><code>preferDist($preferDist = null)</code> adds <code>prefer-dist</code> option to composer</li><li><code>preferSource()</code> adds <code>prefer-source</code> option to composer</li><li><code>dev($dev = null)</code> adds <code>dev</code> option to composer</li><li><code>noDev()</code> adds <code>no-dev</code> option to composer</li><li><code>ansi($ansi = null)</code> adds <code>ansi</code> option to composer</li><li><code>noAnsi()</code> adds <code>no-ansi</code> option to composer</li><li><code>interaction($interaction = null)</code> * <code>param bool</code> $interaction</li><li><code>noInteraction()</code> adds <code>no-interaction</code> option to composer</li><li><code>optimizeAutoloader($optimize = null)</code> adds <code>optimize-autoloader</code> option to composer</li><li><code>ignorePlatformRequirements($ignore = null)</code> adds <code>ignore-platform-reqs</code> option to composer</li><li><code>disablePlugins($disable = null)</code> disable plugins</li><li><code>noScripts($disable = null)</code> skip scripts</li><li><code>workingDir($dir)</code> adds <code>--working-dir $dir</code> option to composer</li><li><code>setOutput($output)</code> Sets the Console Output.</li><li><code>setProcessInput($input)</code> Pass an input to the process. Can be resource created with fopen() or string</li><li><code>dir($dir)</code> Changes working directory of command</li><li><code>arg($arg)</code> Pass argument to executable. Its value will be automatically escaped.</li><li><code>args($args)</code> Pass methods parameters as arguments to executable. Argument values</li><li><code>rawArg($arg)</code> Pass the provided string in its raw (as provided) form as an argument to executable.</li><li><code>option($option, $value = null, $separator = null)</code> Pass option to executable. Options are prefixed with <code>--</code> , value can be provided in second parameter.</li><li><code>options(array $options, $separator = null)</code> Pass multiple options to executable. The associative array contains</li><li><code>optionList($option, $value = null, $separator = null)</code> Pass an option with multiple values to executable. Value can be a string or array.</li></ul><h2 id="dumpautoload" tabindex="-1"><a class="header-anchor" href="#dumpautoload" aria-hidden="true">#</a> DumpAutoload</h2><p>Composer Dump Autoload</p><div class="language-php ext-php line-numbers-mode"><pre class="language-php"><code><span class="token php language-php"><span class="token delimiter important">&lt;?php</span>
<span class="token comment">// simple execution</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerDumpAutoload</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// dump auto loader with custom path</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerDumpAutoload</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;path/to/my/composer.phar&#39;</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">preferDist</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// optimize autoloader dump with custom path</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerDumpAutoload</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;path/to/my/composer.phar&#39;</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">optimize</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// optimize autoloader dump with custom path and no dev</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerDumpAutoload</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;path/to/my/composer.phar&#39;</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">optimize</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">noDev</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token delimiter important">?&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>optimize($optimize = null)</code> * <code>param bool</code> $optimize</li><li><code>preferDist($preferDist = null)</code> adds <code>prefer-dist</code> option to composer</li><li><code>preferSource()</code> adds <code>prefer-source</code> option to composer</li><li><code>dev($dev = null)</code> adds <code>dev</code> option to composer</li><li><code>noDev()</code> adds <code>no-dev</code> option to composer</li><li><code>ansi($ansi = null)</code> adds <code>ansi</code> option to composer</li><li><code>noAnsi()</code> adds <code>no-ansi</code> option to composer</li><li><code>interaction($interaction = null)</code> * <code>param bool</code> $interaction</li><li><code>noInteraction()</code> adds <code>no-interaction</code> option to composer</li><li><code>optimizeAutoloader($optimize = null)</code> adds <code>optimize-autoloader</code> option to composer</li><li><code>ignorePlatformRequirements($ignore = null)</code> adds <code>ignore-platform-reqs</code> option to composer</li><li><code>disablePlugins($disable = null)</code> disable plugins</li><li><code>noScripts($disable = null)</code> skip scripts</li><li><code>workingDir($dir)</code> adds <code>--working-dir $dir</code> option to composer</li><li><code>buildCommand()</code> Copy class fields into command options as directed.</li><li><code>setOutput($output)</code> Sets the Console Output.</li><li><code>setProcessInput($input)</code> Pass an input to the process. Can be resource created with fopen() or string</li><li><code>dir($dir)</code> Changes working directory of command</li><li><code>arg($arg)</code> Pass argument to executable. Its value will be automatically escaped.</li><li><code>args($args)</code> Pass methods parameters as arguments to executable. Argument values</li><li><code>rawArg($arg)</code> Pass the provided string in its raw (as provided) form as an argument to executable.</li><li><code>option($option, $value = null, $separator = null)</code> Pass option to executable. Options are prefixed with <code>--</code> , value can be provided in second parameter.</li><li><code>options(array $options, $separator = null)</code> Pass multiple options to executable. The associative array contains</li><li><code>optionList($option, $value = null, $separator = null)</code> Pass an option with multiple values to executable. Value can be a string or array.</li></ul><h2 id="init" tabindex="-1"><a class="header-anchor" href="#init" aria-hidden="true">#</a> Init</h2><p>Composer Init</p><div class="language-php ext-php line-numbers-mode"><pre class="language-php"><code><span class="token php language-php"><span class="token delimiter important">&lt;?php</span>
<span class="token comment">// simple execution</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerInit</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token delimiter important">?&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>projectName($projectName)</code> * <code>param string</code> $projectName</li><li><code>description($description)</code> * <code>param string</code> $description</li><li><code>author($author)</code> * <code>param string</code> $author</li><li><code>projectType($type)</code> * <code>param string</code> $type</li><li><code>homepage($homepage)</code> * <code>param string</code> $homepage</li><li><code>dependency($project, $version = null)</code> &#39;require&#39; is a keyword, so it cannot be a method name.</li><li><code>stability($stability)</code> * <code>param string</code> $stability</li><li><code>license($license)</code> * <code>param string</code> $license</li><li><code>repository($repository)</code> * <code>param string</code> $repository</li><li><code>preferDist($preferDist = null)</code> adds <code>prefer-dist</code> option to composer</li><li><code>preferSource()</code> adds <code>prefer-source</code> option to composer</li><li><code>dev($dev = null)</code> adds <code>dev</code> option to composer</li><li><code>noDev()</code> adds <code>no-dev</code> option to composer</li><li><code>ansi($ansi = null)</code> adds <code>ansi</code> option to composer</li><li><code>noAnsi()</code> adds <code>no-ansi</code> option to composer</li><li><code>interaction($interaction = null)</code> * <code>param bool</code> $interaction</li><li><code>noInteraction()</code> adds <code>no-interaction</code> option to composer</li><li><code>optimizeAutoloader($optimize = null)</code> adds <code>optimize-autoloader</code> option to composer</li><li><code>ignorePlatformRequirements($ignore = null)</code> adds <code>ignore-platform-reqs</code> option to composer</li><li><code>disablePlugins($disable = null)</code> disable plugins</li><li><code>noScripts($disable = null)</code> skip scripts</li><li><code>workingDir($dir)</code> adds <code>--working-dir $dir</code> option to composer</li><li><code>buildCommand()</code> Copy class fields into command options as directed.</li><li><code>setOutput($output)</code> Sets the Console Output.</li><li><code>setProcessInput($input)</code> Pass an input to the process. Can be resource created with fopen() or string</li><li><code>dir($dir)</code> Changes working directory of command</li><li><code>arg($arg)</code> Pass argument to executable. Its value will be automatically escaped.</li><li><code>args($args)</code> Pass methods parameters as arguments to executable. Argument values</li><li><code>rawArg($arg)</code> Pass the provided string in its raw (as provided) form as an argument to executable.</li><li><code>option($option, $value = null, $separator = null)</code> Pass option to executable. Options are prefixed with <code>--</code> , value can be provided in second parameter.</li><li><code>options(array $options, $separator = null)</code> Pass multiple options to executable. The associative array contains</li><li><code>optionList($option, $value = null, $separator = null)</code> Pass an option with multiple values to executable. Value can be a string or array.</li></ul><h2 id="install" tabindex="-1"><a class="header-anchor" href="#install" aria-hidden="true">#</a> Install</h2><p>Composer Install</p><div class="language-php ext-php line-numbers-mode"><pre class="language-php"><code><span class="token php language-php"><span class="token delimiter important">&lt;?php</span>
<span class="token comment">// simple execution</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerInstall</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// prefer dist with custom path</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerInstall</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;path/to/my/composer.phar&#39;</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">preferDist</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// optimize autoloader with custom path</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerInstall</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;path/to/my/composer.phar&#39;</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">optimizeAutoloader</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token delimiter important">?&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>noSuggest($noSuggest = null)</code> adds <code>no-suggest</code> option to composer</li><li><code>preferDist($preferDist = null)</code> adds <code>prefer-dist</code> option to composer</li><li><code>preferSource()</code> adds <code>prefer-source</code> option to composer</li><li><code>dev($dev = null)</code> adds <code>dev</code> option to composer</li><li><code>noDev()</code> adds <code>no-dev</code> option to composer</li><li><code>ansi($ansi = null)</code> adds <code>ansi</code> option to composer</li><li><code>noAnsi()</code> adds <code>no-ansi</code> option to composer</li><li><code>interaction($interaction = null)</code> * <code>param bool</code> $interaction</li><li><code>noInteraction()</code> adds <code>no-interaction</code> option to composer</li><li><code>optimizeAutoloader($optimize = null)</code> adds <code>optimize-autoloader</code> option to composer</li><li><code>ignorePlatformRequirements($ignore = null)</code> adds <code>ignore-platform-reqs</code> option to composer</li><li><code>disablePlugins($disable = null)</code> disable plugins</li><li><code>noScripts($disable = null)</code> skip scripts</li><li><code>workingDir($dir)</code> adds <code>--working-dir $dir</code> option to composer</li><li><code>buildCommand()</code> Copy class fields into command options as directed.</li><li><code>setOutput($output)</code> Sets the Console Output.</li><li><code>setProcessInput($input)</code> Pass an input to the process. Can be resource created with fopen() or string</li><li><code>dir($dir)</code> Changes working directory of command</li><li><code>arg($arg)</code> Pass argument to executable. Its value will be automatically escaped.</li><li><code>args($args)</code> Pass methods parameters as arguments to executable. Argument values</li><li><code>rawArg($arg)</code> Pass the provided string in its raw (as provided) form as an argument to executable.</li><li><code>option($option, $value = null, $separator = null)</code> Pass option to executable. Options are prefixed with <code>--</code> , value can be provided in second parameter.</li><li><code>options(array $options, $separator = null)</code> Pass multiple options to executable. The associative array contains</li><li><code>optionList($option, $value = null, $separator = null)</code> Pass an option with multiple values to executable. Value can be a string or array.</li></ul><h2 id="remove" tabindex="-1"><a class="header-anchor" href="#remove" aria-hidden="true">#</a> Remove</h2><p>Composer Remove</p><div class="language-php ext-php line-numbers-mode"><pre class="language-php"><code><span class="token php language-php"><span class="token delimiter important">&lt;?php</span>
<span class="token comment">// simple execution</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerRemove</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token delimiter important">?&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>dev($dev = null)</code> * <code>param bool</code> $dev</li><li><code>noProgress($noProgress = null)</code> * <code>param bool</code> $noProgress</li><li><code>noUpdate($noUpdate = null)</code> * <code>param bool</code> $noUpdate</li><li><code>updateNoDev($updateNoDev = null)</code> * <code>param bool</code> $updateNoDev</li><li><code>noUpdateWithDependencies($updateWithDependencies = null)</code> * <code>param bool</code> $updateWithDependencies</li><li><code>preferDist($preferDist = null)</code> adds <code>prefer-dist</code> option to composer</li><li><code>preferSource()</code> adds <code>prefer-source</code> option to composer</li><li><code>noDev()</code> adds <code>no-dev</code> option to composer</li><li><code>ansi($ansi = null)</code> adds <code>ansi</code> option to composer</li><li><code>noAnsi()</code> adds <code>no-ansi</code> option to composer</li><li><code>interaction($interaction = null)</code> * <code>param bool</code> $interaction</li><li><code>noInteraction()</code> adds <code>no-interaction</code> option to composer</li><li><code>optimizeAutoloader($optimize = null)</code> adds <code>optimize-autoloader</code> option to composer</li><li><code>ignorePlatformRequirements($ignore = null)</code> adds <code>ignore-platform-reqs</code> option to composer</li><li><code>disablePlugins($disable = null)</code> disable plugins</li><li><code>noScripts($disable = null)</code> skip scripts</li><li><code>workingDir($dir)</code> adds <code>--working-dir $dir</code> option to composer</li><li><code>buildCommand()</code> Copy class fields into command options as directed.</li><li><code>setOutput($output)</code> Sets the Console Output.</li><li><code>setProcessInput($input)</code> Pass an input to the process. Can be resource created with fopen() or string</li><li><code>dir($dir)</code> Changes working directory of command</li><li><code>arg($arg)</code> Pass argument to executable. Its value will be automatically escaped.</li><li><code>args($args)</code> Pass methods parameters as arguments to executable. Argument values</li><li><code>rawArg($arg)</code> Pass the provided string in its raw (as provided) form as an argument to executable.</li><li><code>option($option, $value = null, $separator = null)</code> Pass option to executable. Options are prefixed with <code>--</code> , value can be provided in second parameter.</li><li><code>options(array $options, $separator = null)</code> Pass multiple options to executable. The associative array contains</li><li><code>optionList($option, $value = null, $separator = null)</code> Pass an option with multiple values to executable. Value can be a string or array.</li></ul><h2 id="requiredependency" tabindex="-1"><a class="header-anchor" href="#requiredependency" aria-hidden="true">#</a> RequireDependency</h2><p>Composer Require</p><div class="language-php ext-php line-numbers-mode"><pre class="language-php"><code><span class="token php language-php"><span class="token delimiter important">&lt;?php</span>
<span class="token comment">// simple execution</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerRequire</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">dependency</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;foo/bar&#39;</span><span class="token punctuation">,</span> <span class="token string single-quoted-string">&#39;^.2.4.8&#39;</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token delimiter important">?&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>dependency($project, $version = null)</code> &#39;require&#39; is a keyword, so it cannot be a method name.</li><li><code>noSuggest($noSuggest = null)</code> adds <code>no-suggest</code> option to composer</li><li><code>preferDist($preferDist = null)</code> adds <code>prefer-dist</code> option to composer</li><li><code>preferSource()</code> adds <code>prefer-source</code> option to composer</li><li><code>dev($dev = null)</code> adds <code>dev</code> option to composer</li><li><code>noDev()</code> adds <code>no-dev</code> option to composer</li><li><code>ansi($ansi = null)</code> adds <code>ansi</code> option to composer</li><li><code>noAnsi()</code> adds <code>no-ansi</code> option to composer</li><li><code>interaction($interaction = null)</code> * <code>param bool</code> $interaction</li><li><code>noInteraction()</code> adds <code>no-interaction</code> option to composer</li><li><code>optimizeAutoloader($optimize = null)</code> adds <code>optimize-autoloader</code> option to composer</li><li><code>ignorePlatformRequirements($ignore = null)</code> adds <code>ignore-platform-reqs</code> option to composer</li><li><code>disablePlugins($disable = null)</code> disable plugins</li><li><code>noScripts($disable = null)</code> skip scripts</li><li><code>workingDir($dir)</code> adds <code>--working-dir $dir</code> option to composer</li><li><code>buildCommand()</code> Copy class fields into command options as directed.</li><li><code>setOutput($output)</code> Sets the Console Output.</li><li><code>setProcessInput($input)</code> Pass an input to the process. Can be resource created with fopen() or string</li><li><code>dir($dir)</code> Changes working directory of command</li><li><code>arg($arg)</code> Pass argument to executable. Its value will be automatically escaped.</li><li><code>args($args)</code> Pass methods parameters as arguments to executable. Argument values</li><li><code>rawArg($arg)</code> Pass the provided string in its raw (as provided) form as an argument to executable.</li><li><code>option($option, $value = null, $separator = null)</code> Pass option to executable. Options are prefixed with <code>--</code> , value can be provided in second parameter.</li><li><code>options(array $options, $separator = null)</code> Pass multiple options to executable. The associative array contains</li><li><code>optionList($option, $value = null, $separator = null)</code> Pass an option with multiple values to executable. Value can be a string or array.</li></ul><h2 id="update" tabindex="-1"><a class="header-anchor" href="#update" aria-hidden="true">#</a> Update</h2><p>Composer Update</p><div class="language-php ext-php line-numbers-mode"><pre class="language-php"><code><span class="token php language-php"><span class="token delimiter important">&lt;?php</span>
<span class="token comment">// simple execution</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerUpdate</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// prefer dist with custom path</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerUpdate</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;path/to/my/composer.phar&#39;</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">preferDist</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// optimize autoloader with custom path</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerUpdate</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;path/to/my/composer.phar&#39;</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">optimizeAutoloader</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
     <span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token delimiter important">?&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>noSuggest($noSuggest = null)</code> adds <code>no-suggest</code> option to composer</li><li><code>preferDist($preferDist = null)</code> adds <code>prefer-dist</code> option to composer</li><li><code>preferSource()</code> adds <code>prefer-source</code> option to composer</li><li><code>dev($dev = null)</code> adds <code>dev</code> option to composer</li><li><code>noDev()</code> adds <code>no-dev</code> option to composer</li><li><code>ansi($ansi = null)</code> adds <code>ansi</code> option to composer</li><li><code>noAnsi()</code> adds <code>no-ansi</code> option to composer</li><li><code>interaction($interaction = null)</code> * <code>param bool</code> $interaction</li><li><code>noInteraction()</code> adds <code>no-interaction</code> option to composer</li><li><code>optimizeAutoloader($optimize = null)</code> adds <code>optimize-autoloader</code> option to composer</li><li><code>ignorePlatformRequirements($ignore = null)</code> adds <code>ignore-platform-reqs</code> option to composer</li><li><code>disablePlugins($disable = null)</code> disable plugins</li><li><code>noScripts($disable = null)</code> skip scripts</li><li><code>workingDir($dir)</code> adds <code>--working-dir $dir</code> option to composer</li><li><code>buildCommand()</code> Copy class fields into command options as directed.</li><li><code>setOutput($output)</code> Sets the Console Output.</li><li><code>setProcessInput($input)</code> Pass an input to the process. Can be resource created with fopen() or string</li><li><code>dir($dir)</code> Changes working directory of command</li><li><code>arg($arg)</code> Pass argument to executable. Its value will be automatically escaped.</li><li><code>args($args)</code> Pass methods parameters as arguments to executable. Argument values</li><li><code>rawArg($arg)</code> Pass the provided string in its raw (as provided) form as an argument to executable.</li><li><code>option($option, $value = null, $separator = null)</code> Pass option to executable. Options are prefixed with <code>--</code> , value can be provided in second parameter.</li><li><code>options(array $options, $separator = null)</code> Pass multiple options to executable. The associative array contains</li><li><code>optionList($option, $value = null, $separator = null)</code> Pass an option with multiple values to executable. Value can be a string or array.</li></ul><h2 id="validate" tabindex="-1"><a class="header-anchor" href="#validate" aria-hidden="true">#</a> Validate</h2><p>Composer Validate</p><div class="language-php ext-php line-numbers-mode"><pre class="language-php"><code><span class="token php language-php"><span class="token delimiter important">&lt;?php</span>
<span class="token comment">// simple execution</span>
<span class="token variable">$this</span><span class="token operator">-&gt;</span><span class="token function">taskComposerValidate</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token delimiter important">?&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>noCheckAll($noCheckAll = null)</code> * <code>param bool</code> $noCheckAll</li><li><code>noCheckLock($noCheckLock = null)</code> * <code>param bool</code> $noCheckLock</li><li><code>noCheckPublish($noCheckPublish = null)</code> * <code>param bool</code> $noCheckPublish</li><li><code>withDependencies($withDependencies = null)</code> * <code>param bool</code> $withDependencies</li><li><code>strict($strict = null)</code> * <code>param bool</code> $strict</li><li><code>preferDist($preferDist = null)</code> adds <code>prefer-dist</code> option to composer</li><li><code>preferSource()</code> adds <code>prefer-source</code> option to composer</li><li><code>dev($dev = null)</code> adds <code>dev</code> option to composer</li><li><code>noDev()</code> adds <code>no-dev</code> option to composer</li><li><code>ansi($ansi = null)</code> adds <code>ansi</code> option to composer</li><li><code>noAnsi()</code> adds <code>no-ansi</code> option to composer</li><li><code>interaction($interaction = null)</code> * <code>param bool</code> $interaction</li><li><code>noInteraction()</code> adds <code>no-interaction</code> option to composer</li><li><code>optimizeAutoloader($optimize = null)</code> adds <code>optimize-autoloader</code> option to composer</li><li><code>ignorePlatformRequirements($ignore = null)</code> adds <code>ignore-platform-reqs</code> option to composer</li><li><code>disablePlugins($disable = null)</code> disable plugins</li><li><code>noScripts($disable = null)</code> skip scripts</li><li><code>workingDir($dir)</code> adds <code>--working-dir $dir</code> option to composer</li><li><code>buildCommand()</code> Copy class fields into command options as directed.</li><li><code>setOutput($output)</code> Sets the Console Output.</li><li><code>setProcessInput($input)</code> Pass an input to the process. Can be resource created with fopen() or string</li><li><code>dir($dir)</code> Changes working directory of command</li><li><code>arg($arg)</code> Pass argument to executable. Its value will be automatically escaped.</li><li><code>args($args)</code> Pass methods parameters as arguments to executable. Argument values</li><li><code>rawArg($arg)</code> Pass the provided string in its raw (as provided) form as an argument to executable.</li><li><code>option($option, $value = null, $separator = null)</code> Pass option to executable. Options are prefixed with <code>--</code> , value can be provided in second parameter.</li><li><code>options(array $options, $separator = null)</code> Pass multiple options to executable. The associative array contains</li><li><code>optionList($option, $value = null, $separator = null)</code> Pass an option with multiple values to executable. Value can be a string or array.</li></ul>`,41),t=[i];function c(l,d){return o(),n("div",null,t)}var r=e(a,[["render",c],["__file","Composer.html.vue"]]);export{r as default};
